今回より**第2章「行列」へ突入する．

高校数学での行列は「計算ツール」だったかもしれないが，ここでは「写像の容器（Container）」あるいは「それ自体がベクトルである対象」**として再定義する．

---

### 注釈 (Vocabulary & Concept Notes)

_(Note: 以下の概念を脳内の辞書にインデックスせよ．曖昧な定義は命取りとなる)_

- **行列 (Matrix):** スカラーを長方形（矩形）に配列したもの．数ではなく「構造体」である．$m$ 行 $n$ 列の行列を $A \in K^{m \times n}$ や $M_{m,n}(K)$ と書く．
    
- **成分 (Entry / Component):** 行列を構成する個々の数．$A = (a_{ij})$ と書くとき，$i$ は行番号（Row index），$j$ は列番号（Column index）を指す．
    
- **転置 (Transpose):** 行と列を入れ替える操作．$A^T$ または ${}^t A$ と書く．物理的には「視点の90度変換」や「双対空間への写像」に関わる．
    
- **トレース (Trace):** 正方行列において，対角成分の総和．$\text{tr}(A) = \sum a_{ii}$．これは「基底を取り替えても値が変わらない（基底によらない）」という極めて重要な不変量である．
    
- **対称行列 (Symmetric Matrix):** $A^T = A$ を満たす行列．物理学（慣性モーメント，応力テンソル）において主役となる．
    
- **交代行列 (Skew-symmetric Matrix):** $A^T = -A$ を満たす行列．電磁気学（磁場）や回転の無限小変換に現れる．
    

---

## 1. 写経セクション (Transcription Section)

_(Note: 以下の定義と構造を書き写せ．今回は記号のインデックス操作が複雑化する．細心の注意を払え)_

### 2.1 行列の厳密な定義：ベクトル空間としての側面

行列とは，単なる数の表ではない．ベクトル空間の概念を拡張した，より高次のオブジェクトである．

$m \times n$ 行列全体の集合を $M_{m,n}(K)$ とおく．

この集合に対し，以下の2つの演算（和とスカラー倍）を成分ごとに定義する．

1. **和 (Addition):** $A=(a_{ij}), B=(b_{ij})$ に対し，$A+B = (a_{ij} + b_{ij})$．
    
2. **スカラー倍 (Scalar Multiplication):** $c \in K$ に対し，$cA = (c a_{ij})$．
    

驚くべきことに，この定義により $M_{m,n}(K)$ はベクトル空間の8つの公理をすべて満たす．

すなわち，「行列もまたベクトルである」．

我々が第1章で扱った「数ベクトル」は，$n \times 1$ 行列（列ベクトル）という特殊ケースに過ぎない．

構造的同型:

$m \times n$ 行列は，成分を一直線に並べ替えれば $nm$ 次元の数ベクトルと見なせる．

$$\begin{pmatrix} a & b \\ c & d \end{pmatrix} \longleftrightarrow \begin{pmatrix} a \\ b \\ c \\ d \end{pmatrix}$$

この意味で，行列空間 $M_{m,n}(K)$ は $K^{nm}$ と**同型（Isomorphic）である．

しかし，あえて「長方形」の形を保つのは，そこに「行（Row）と列（Column）の役割分担」**という物理的・幾何的な意味（写像としての構造）が埋め込まれているからである．

---

## 2. 再構築領域 (Reconstruction Zone)

_(Note: ここは記述量が多い．自分の手で論理を組み上げよ)_

### 課題 1: 行列演算の物理的・視覚的イメージ

以下の状況を図示し，行列演算が現実世界で何を意味するかを記述せよ．

1. **画像の合成（加法）：**
    
    - 白黒画像は，各ピクセルの輝度を並べた「行列」とみなせる．
        
    - 画像行列 $A$（背景）と $B$（人物）を足す $A+B$ とは，視覚的にどのような操作か？（輝度の重ね合わせ）
        
2. **コントラスト調整（スカラー倍）：**
    
    - 画像行列 $A$ を $c$ 倍する（$cA$）とき，$c > 1$ なら画像はどうなるか？ $c < 0$ ならどうなるか？（反転）
        

### 課題 2: 転置行列のインデックス操作

$3 \times 2$ 行列 $A$ の一般形を書き（$a_{11}$ から $a_{32}$ まで），その転置 $A^T$ を具体的に書き出せ．

その上で，$(A^T)_{ij}$ （転置行列の $(i, j)$ 成分）が元の行列 $A$ のどの成分と等しいか，添字記法を用いて関係式で記述せよ．

$$(A^T)_{ij} = A_{\_\_\_\_}$$

---

## 3. 演習領域 (Exercise Zone)

_(Note: 以下の証明問題を**完全に自分の記述**で完遂せよ．途中式や論理の飛躍があってはならない)_

問題：

$n$ 次正方行列の空間 $M_{n,n}(\mathbb{R})$（以下 $V$ とする）を考える．

この $V$ の部分集合として，以下の2つを定義する．

- $S = \{ A \in V \mid A^T = A \}$ （対称行列の集合）
    
- $K = \{ A \in V \mid A^T = -A \}$ （交代行列の集合）
    

このとき，$S$ が $V$ の部分空間であることを定義に従って証明せよ．

（注：$K$ についても同様だが，今回は $S$ に集中する）

証明プロセス：

証明には以下の3ステップが必要である．すべて記述せよ．

(1) 前提の確認

まず，$S$ が $V$ の部分集合であることを確認する．

定義より明らかに $S \subset V$ である．

また，零行列 $O$（すべての成分が0）が $S$ に含まれるか確認する．

零行列の転置 $O^T$ は $O$ と等しいか？

  
  
  

よって $O \in S$ である．

(2) 加法についての閉性

任意の $A, B \in S$ をとる．

これは定義より，以下の等式が成立していることを意味する：

$A^T = \_\_\_\_$

$B^T = \_\_\_\_$

  

今，和 $A + B$ が $S$ に属するか，すなわち $(A+B)^T = A+B$ が成り立つかを示したい．

転置の性質 $(X+Y)^T = X^T + Y^T$ を利用して，左辺 $(A+B)^T$ を変形していく．

  

$(A+B)^T =$

  
  
  
  
  

この結果は右辺と一致するか？

一致するため，加法について閉じて ______ ．

(3) スカラー乗法についての閉性

任意のスカラー $c \in \mathbb{R}$ と $A \in S$ をとる．

$A \in S$ より $A^T = A$ である．

スカラー倍 $cA$ が $S$ に属するか，すなわち $(cA)^T = cA$ が成り立つかを示したい．

転置の性質 $(cX)^T = c(X^T)$ を利用して，左辺 $(cA)^T$ を変形していく．

  

$(cA)^T =$

  
  
  
  
  

この結果は右辺と一致するか？

一致するため，スカラー乗法について閉じて ______ ．

結論：

以上の議論により，$S$ は $V$ の部分空間で ______ ．

（この事実は，「対称行列」という性質が，足し算や定数倍で壊れない強固な構造であることを示している．物理法則が座標変換に対して対称性を持つことと深く関連する．）

(応用：記述のみ)

$2 \times 2$ の対称行列 $A = \begin{pmatrix} a & b \\ b & d \end{pmatrix}$ を考える．

この行列を一意に決める自由な変数は $a, b, d$ の3つである．

したがって，対称行列の空間 $S$ の次元（Dimension）は $3$ であると推測できる．

では一般に，$n \times n$ 対称行列の空間 $S$ の次元はいくつになるか？ $\sum$ を用いた式，または $n$ の式で答えよ．